# アップデート手順（前のバージョンから乗り換える）

このツールは「フォルダごと新しいものに置き換える」方式でアップデートします。

あなたの設定（登録済みアカウント・コンセプトシート・作った投稿・キュー・クラウドオフロード設定）は、
**前のフォルダの中の `prisma/dev.db` という1ファイルに全部入っています**。それを新しいフォルダに引き継ぐだけです。
コンセプト設定などをやり直す必要はありません。

> まだ一度もセットアップしていない方へ：引き継ぐデータがないので、このページは無視して、
> 普通に `bash setup.sh` を実行すればOKです。

---

## いちばん簡単な方法（おすすめ）

1. この新しいzipを、**今まで使っているフォルダとは「別の場所」に展開**してください（例: デスクトップ）
2. 展開してできたフォルダの中にある **`アップデート.command`（Mac）/ `アップデート.bat`（Windows）をダブルクリック** します
   - ターミナルが自動で開いて、更新が始まります（ターミナルを自分で開く必要はありません）
   - ※ Macで「開発元を検証できないため開けません」と出たら、その `.command` ファイルを **右クリック →「開く」→「開く」** を押してください（最初の1回だけ）
3. 「前のフォルダのパスは？」と聞かれるので、**今まで使っていたフォルダを、その開いたターミナルの窓にドラッグ&ドロップ** → Enter
4. あとは自動で「データ引き継ぎ → 更新」が走ります。最後に「いま起動しますか？」と出たら `y` を押してください。
5. ブラウザが開いて、今までのアカウント・コンセプトがそのまま表示されていれば完了です。

> Claude Code を使っている場合は、新しいフォルダで Claude Code に「**アップデートして**」と頼めば、
> 上の手順を代わりにやってくれます（前のフォルダの場所だけ聞かれます）。
>
> 自分でターミナルを操作したい人は、新しいフォルダの中でターミナルを開いて `bash update.sh` を実行してもOKです（同じことが起きます）。

---

## 手動でやりたい場合

1. 新しいzipを別フォルダに展開する
2. **前のフォルダの `prisma/dev.db` を、新しいフォルダの `prisma/` フォルダの中にコピー（上書き）**する
3. 新しいフォルダで、ターミナルから次を順に実行：

   ```
   npm install
   npx prisma generate
   npx prisma migrate deploy
   ```

   ※ `bash setup.sh` でも動きますが、setup.sh は「既定ナレッジ」を初期内容に戻します。
   既定ナレッジを自分で編集している場合は、上の3コマンド（または `bash update.sh`）を使ってください。
4. `bash start.sh` で起動

---

## 注意

- 前のフォルダは、念のためしばらく残しておいてください（バックアップになります）。新しい方がちゃんと動くのを確認できたら削除してOKです。
- `update.sh` は、引き継ぐ前に「新しいフォルダにあった dev.db」を `prisma/dev.db.bak.（日時）` という名前で自動退避します。なので失敗しても元に戻せます。
- クラウドオフロード（PCを閉じても投稿する機能）を設定していた方も、その設定（スプレッドシート・GASのURL・トークン）は `dev.db` に入っているので、上の手順でそのまま引き継がれます。
  - ただし**1つだけ追加でやること**があります：アップデートが終わったら、もう一度 **`クラウドオフロード設定.command`（Mac）/ `クラウドオフロード設定.bat`（Windows）をダブルクリック**（or Claude Code に「クラウドオフロードをセットアップして」）→ 聞かれたら **「① GASコードを最新に更新する」を選ぶ**。これで Google 側に置いてあるコードも最新になります（スプレッドシート・URL は変わりません。数十秒で終わります）。
  - ※ クラウドオフロードを設定していない方は、これは不要です。
- うまくいかないときは、購入者向けチャンネルで気軽に聞いてください。
