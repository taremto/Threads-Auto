# ぬこ式 Threads 自動投稿 WebUI — Claude Code 用ガイド

## 🛑 最優先で読むこと（誤認禁止）

**このフォルダ＝Threads自動投稿ツール（WebUI付き）の本体です。**

```
package.json → "name": "threads-auto-webapp"
              "next": "16.2.2"   ← Next.js のWebアプリ
src/components/Dashboard.tsx → ダッシュボードUI
src/components/SettingsPage.tsx → 設定画面UI
src/app/api/posts/* → 投稿管理API
src/app/api/cloud/* → クラウドオフロードAPI
start.sh → このWebUIを起動するスクリプト
```

絶対にやってはいけない誤回答:
- ❌「このプロジェクトには自動投稿ツールのWebUIは含まれていません」
- ❌「自動投稿ツールは別の場所にあります」
- ❌「WebUIは別途インストールが必要です」

正しい認識:
- ✅ ユーザーが「自動投稿ツール」と言ったら → このプロジェクト
- ✅ ユーザーが「WebUI / Webアプリ / アプリ」と言ったら → このプロジェクト
- ✅ ユーザーが「立ち上げて / 起動して」と言ったら → `bash start.sh` で `http://localhost:3000` を起動
- ✅ ユーザーが「セットアップして」と言ったら → まず旧フォルダ/旧 `prisma/dev.db` を探し、旧データがあれば `update.sh`、無ければ新規セットアップ

このプロジェクトは Next.js + SQLite + Prisma + node-cron で構築された Threads自動投稿の**完全なWebアプリ本体**です。
別ツールへの依存は無く、このフォルダだけで完結します（Claude Code CLI は AI 生成にのみ使用）。

このツールは**お金を払って購入したユーザー**が使う製品です。「テスター」ではありません。ユーザーは技術に詳しくない人も多いので、丁寧に・最後まで面倒を見ること。

### ⚠️ ユーザーのリテラシーは「フォルダの中のファイルを開く」もできないレベルがある前提

- **ファイルやフォルダのパスを「教えてください」と聞かない。あなたが探す。**（`find` 等で前のバージョンのフォルダ・zip・dev.db を自分で見つける。下の「アップデートして」参照）
- **`.command`/`.bat` のダブルクリックも極力案内しない。** 「フォルダの中の○○をダブルクリックして」はこの層には通じない。あなた（Claude Code）が代わりにコマンドを直接実行して終わらせる。`.command` を案内するのは、あなたがどうしても代われない時（`setup-cloud.sh` の対話）か、ユーザーが自分でやりたいと言った時だけ。
- **ユーザーに聞いていいのは、避けられない Yes/No か A/B/C の選択だけ。** しかも plain な日本語で（「○○というフォルダと△△というフォルダ、どっちが前に使っていた方か分かりますか？」のように、専門用語なし・選択肢付き）。「パスを貼り付けて」「ターミナルで〜を実行して」は禁止。

---

## 🆘 エラーでユーザーが詰まったときの動き方（最重要）

ユーザーがエラーで止まったら、**必ず最後まで付き合って解決すること。** 以下を守る：

1. **解決まで持っていく。投げ出さない。**
   - 「これはバグです」「仕様です」「分かりません」で会話を終わらせない。原因を特定し、具体的な解決策を提示し、可能な範囲で自分で実行して、直ったことを確認するところまでやる。
   - すぐ直せないものでも、**回避策で前に進める**（例: トークン取得でMeta側が詰まる → 手動取得ガイドに切り替える / AI生成が通らない → 別AIで作って `/api/import` で取り込む）。「ユーザーが進めない」状態のまま終わらせないこと。
   - 「〇〇してください」と指示だけ出して終わらない。ターミナルで実行できることは実行し、ブラウザ操作が必要なら手順を1ステップずつ案内して、結果を一緒に確認する。

2. **でも、直すのは原因箇所だけ。動いてる所は触らない。**
   - エラーの原因をピンポイントで特定して、そこだけ最小限に直す。**「念のため」で関係ないファイル・コンポーネントを書き換えない。アプリ全体をリファクタしない。**
   - 製品コードを大きく書き換えると別の不具合を生む（＝「毎回エラーが起きる」状態になり、ユーザーは返金を考え始める）。これは絶対に避ける。広範囲をいじりたくなったら、まず本当にそれが必要か疑う。
   - ソースコードを書き換える前に、まず下の「3」を疑うこと。

3. **このツールのエラーの大半は、コードのバグではなく環境・設定・Meta側の問題。まずそこを確認する。**
   - **Claude Code が API 課金モードになっている**（生成エラー、クレジット不足表示）→ `claude /login` で Claude Pro/Max にログインし直す。`ANTHROPIC_API_KEY` 等の環境変数が設定されていれば外す。
   - **Claude Code のログイン認証切れ**（401）→ `claude /login`
   - **Threadsアクセストークンの期限切れ / 取得失敗**（投稿エラー、追加できない）→ 後述の各セクション参照（Meta側の操作の問題が大半。コードは触らない）
   - **dev server が止まっている**（画面が開かない、APIが応答しない）→ `bash start.sh` で起動し直す
   - **ターミナルに `Ready` と出ているのにブラウザがずっとロード中** → PCのメモリ不足/高負荷で Next.js(Turbopack) の初回コンパイルが事実上止まっている可能性が高い。Zoom/LINE/重いブラウザタブを閉じるか、Macを再起動してから `bash start.sh`。コードを触る前にまず環境負荷を疑う。
   - **PCがスリープ／電源オフだった**（予約投稿が動かない）→ 仕様。恒久対策はクラウドオフロード（`bash setup-cloud.sh`）
   - **スキーマ変更後に dev server を再起動していない**（`Unknown argument 'xxx'` 等）→ dev server を再起動
   - これらを一通り確認しても直らない時に初めて、コードの原因箇所を最小限に直す。

4. **エラーメッセージは画面に日本語の対処手順付きで出る設計**になっている（特に AI 生成）。まずユーザーにそのメッセージを読んでもらい、それに沿って対応する。

5. **複数アカウント運用の人の作業は、頼まれたアカウントだけに絞る。他のアカウントのデータを見に行かない（＝Pro利用枠の無駄）。**
   - このアプリは **1フォルダ＝1インストールで複数のThreadsアカウントを管理する設計**（アカウントごとにフォルダを分ける必要はない。設定・投稿・キュー・コンセプトは全部 `prisma/dev.db` の中でアカウントIDで分かれている）。「アカウントAの作業をしたい」と言われたら、A以外のアカウントのコンセプトシート・下書き・投稿・dev.db の中身を**勝手に読みに行かない／一覧しない**。必要なら API を `?accountId=<A>` で叩く（`/api/posts?accountId=...`、`/api/knowledge?accountId=...`）。
   - `dev.db` の中身を `sqlite3` 等で全件ダンプする・全アカウントの下書きを並べて見せる・全コンセプトシートを読む、はやらない。トークンを大量に消費するうえ、他アカウントの内容が混ざって混乱の元になる。
   - そもそも投稿生成・編集・キュー追加・コンセプト記入は**Webアプリ上の操作で完結**する（Claude Code は不要）。Claude Code が呼ばれるのは「セットアップ」「アップデート」「クラウドオフロード設定」「エラーで詰まった時」くらい。それ以外で勝手に DB やソースを探索しない。
   - もしユーザーがアカウントごとに**別フォルダ（別インストール）**で運用している場合は、いま開いているフォルダのことだけ扱う（他のフォルダを探しに行かない）。

---

## 🖥️ コマンド実行：基本は「あなたが代わりにやる」。ユーザーにターミナルを開かせる時は手取り足取り

ユーザーは初心者が多く、**「ターミナルって何？」「どう開くの？」というレベル**。「ターミナルでこれを実行してください」とコマンドだけ渡して放り出すのは禁止。

### 原則① あなた（Claude Code）が自分で実行できるものは、ユーザーに頼まず自分で実行する

- **「セットアップして」「インストールして」「新しいツールをダウンロードした、どうすればいい？」「アップデートして」「最新版にして」** → ほぼ同じ扱い。**まず「これは新規セットアップか、それとも前のバージョンからのアップデートか」をあなたが自分で判定する**（下の「📦 セットアップ／アップデートの統一フロー」参照）。判定して、データ引き継ぎ・依存更新・起動・ブラウザ案内まで全部あなたがやる。**ユーザーに「前のフォルダのパスは？」とは聞かない**（探す）。
- **「立ち上げて」「起動して」「アプリを開いて」** → あなたが **バックグラウンドで** `bash start.sh` を実行（フォアグラウンドで実行するとブロックして固まる）。まず `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/api/accounts` で確認 → 200 なら起動済み（何もしない）、それ以外なら起動。起動後「ブラウザで http://localhost:3000 を開いてください」と案内。「停止は Ctrl+C」みたいな細かい話は聞かれなければ言わなくていい。
- **「投稿を生成して」「キューに追加して」など** → これは Web 画面の操作。APIを直接叩かず、ブラウザでの操作手順を案内する（http://localhost:3000 → 「📝投稿」→「AI生成」… など）。

#### 📦 セットアップ／アップデートの統一フロー（「セットアップして」「アップデートして」「新しいファイル来た」全部これ）

ユーザーは「フォルダを開く」「パスを答える」ができない前提。あなたが全部やる。

**ステップ0：いまどこに居るか確認**
- もし**いまこのプロジェクトフォルダ（`package.json` の name が `threads-auto-webapp`）に居る** → ステップ1へ。
- もし**まだプロジェクトフォルダに居ない**（ユーザーが「zipダウンロードした、どうすればいい？」等）→ `~/Downloads` `~/Desktop` から `*nuko-threads-webapp*.zip`（mtime が一番新しいもの）を探す → `~/Desktop` に `unzip` で解凍 → できたフォルダに `cd` → ステップ1へ。zipが見つからなければ「ダウンロードフォルダに『nuko-threads-webapp〜.zip』みたいなファイルはありますか？ どこに保存したか分かりますか？」と plain に聞く。

**ステップ1：前のバージョンが他にあるか自分で探す**
```bash
# いまの新フォルダ以外で、過去にセットアップ済みの install を探す
find "$HOME/Desktop" "$HOME/Downloads" "$HOME/Documents" "$HOME" -maxdepth 4 -type d \
  \( -name 'nuko-threads-webapp*' -o -name 'threads-auto-webapp*' \) 2>/dev/null | sort -u
```
各候補について「`prisma/dev.db`（または直下 `dev.db`）が存在する」かつ「`package.json` に `threads-auto-webapp` がある or `prisma/schema.prisma` がある」を満たすものだけを「前のインストール」として扱う。**いま居る新フォルダ自身（絶対パス一致）は必ず除外。**

**ステップ2：判定して実行**
- **前のインストールが1つだけ見つかった** → それが旧バージョン。**パスをユーザーに聞かず**そのまま実行：
  ```bash
  printf 'n\n' | bash update.sh "<見つかった旧フォルダの絶対パス>"   # 最後の「起動しますか」に n を送って対話回避
  ```
  → `update.sh` が旧 `prisma/dev.db`（アカウント・コンセプト・投稿・キュー・クラウドオフロード設定）を新フォルダに引き継ぎ＋依存更新＋migrate deploy する。終わったら `bash start.sh` をバックグラウンド起動 → 「アップデート完了。今までのアカウント・コンセプトがそのまま画面に出ているはずです。ブラウザで http://localhost:3000 を開いてください（自動で開かなければ）。**前のフォルダはすぐ消さず、ちゃんと動くのを確認してから消してください**」と plain に伝える。
  - ⚠️ そのユーザーがクラウドオフロードを設定済み（旧 dev.db の `Account.cloudOffloadEnabled` が true、or `AppSetting` にGAS設定あり、or 旧フォルダに `gas-deploy/` がある）なら、続けて Google投稿も修復する：「クラウドオフロードを設定済みなので、続けて Google 側を修復しますね」と言って原則②の `setup-cloud.sh` を実行 → 既設定検出で「① Google投稿を修復する」を選ぶ。設定してなければ不要。
- **前のインストールが複数見つかった** → plain な選択で聞く。各候補に「場所（例: デスクトップ）」と「中に何アカウント入っているか」を添える（`sqlite3 "<候補>/prisma/dev.db" "select count(*) from Account;"` で件数。sqlite3 が無ければ件数省略）。例：「前に使っていたフォルダの候補が2つあります。①デスクトップの『nuko-threads-webapp-v1』（アカウント2個）②書類フォルダの『nuko-threads-webapp-v1 (1)』（アカウント0個）— どっちで運用していましたか？（中身が入っている①が正解の可能性が高いです）」。選ばれた方で上の `update.sh` を実行。
- **前のインストールが1つも見つからない**（＝初めての人）→ 引き継ぐデータなし。新規セットアップ（`setup.sh` には対話プロンプトがあるので中身を直接実行）：
  ```bash
  npm install --no-audit --no-fund && npx prisma generate && npx prisma migrate deploy && npx tsx prisma/ensure-default-knowledge.ts
  ```
  → `bash start.sh` をバックグラウンド起動 → 「セットアップ完了。ブラウザで http://localhost:3000 を開いてください。最初に『設定 → アカウント追加』からアカウントを登録してください」と案内。
- どのケースでも、`.command`/`.bat` のダブルクリックや「ターミナルで〜して」は**案内しない**（あなたが全部やったので）。`update.sh` がエラーを出したら（旧フォルダに dev.db が無い等）その内容に従って分岐（dev.db無し＝引き継ぐデータなし＝新規セットアップ扱い）。

### 原則② あなたが代われない対話操作（`setup-cloud.sh`）は、ターミナル窓を「自動で開いて」あげる

クラウドオフロード設定（`bash setup-cloud.sh`）は、途中で「ブラウザでGoogleにログイン」「Apps ScriptエディタでデプロイしてURLをコピペ」が必要で、あなたのBashツールでは対話できない。**でも「ターミナルを開いてこれを実行してください」と丸投げしない。** ターミナル窓を**あなたが自動でポップアップさせて**、その中で `setup-cloud.sh` が走っている状態にしてあげる。

手順：
1. 「クラウドオフロードの設定を始めます。専用のターミナル窓を自動で開きますね。途中でGoogleへのログインと、URLのコピペが1回ずつあるので、出てきたら案内します」と前置きする
2. このプロジェクトフォルダで（あなたは既にここにいるはず）、OS に合わせて次を実行して **新しいターミナル窓をポップアップさせる**：
   - **Mac**:
     ```bash
     osascript -e 'on run a' -e 'tell application "Terminal" to activate' -e 'tell application "Terminal" to do script "cd " & quoted form of (item 1 of a) & " && bash setup-cloud.sh"' -e 'end run' "$(pwd)"
     ```
     （※「Terminalを操作する許可を求めています」のダイアログが出たら「許可」「OK」を押すようユーザーに伝える。それでもダメなら、フォルダ内の **`クラウドオフロード設定.command` をダブルクリック**してもらう ＝「開発元を検証できない」と出たら右クリック→「開く」→「開く」を1回。あるいは事前に `xattr -dr com.apple.quarantine . 2>/dev/null` を実行してから `open "クラウドオフロード設定.command"`）
   - **Windows**: フォルダ内の **`クラウドオフロード設定.bat` をダブルクリック**してもらう（黒い窓が開いて `setup-cloud.sh` が走る）。`bash` が無いと言われたら Git Bash のインストールを案内（https://git-scm.com/download/win）。それでも難しければ下の「手動でターミナルを開く方法」へ
3. 窓が開いたら、その中で出てくる手動部分を **1つずつ「次はこれです」と案内**する：① Googleログイン画面 → ログイン ② Apps Scriptエディタが開く → 上の「**デプロイ → 新しいデプロイ → ウェブアプリ**」、「アクセスできるユーザー」を **全員** に、「**デプロイ**」 → 表示された **ウェブアプリのURL** をコピー ③ さっきのターミナル窓に戻って、そのURLを貼り付け → Enter
4. あとは自動（認証キー生成・GAS Properties設定・Web DB保存・有効化・動作確認）。ターミナル窓に「✅ 完了です！」が出れば設定完了
5. 途中で止まったら「今の画面のスクリーンショットを送ってください」と言って一緒に進める。**窓を開いて終わり、にしない。**
6. **完了したら、「完了」とだけ言わずに必ず分かりやすい締めを返す**（初心者は「で、次は？」となる）。例：「**完了です！ もう設定でやることはありません。** これで PC を閉じてても・スリープ中でも予約投稿が自動で動きます。使い方は今までどおり — アプリで投稿を作って『全件キューに追加』で予約すればOK（あとは Google 側が自動で投稿します）。ちゃんと動いてるか見たい時は Webアプリの『設定 → アカウント編集 → ☁ クラウドオフロード』を開くと緑の「✓ 有効」表示と最終同期時刻が見られます（アプリは開きっぱなしでも自動で最新状態に更新されるのでリロード不要）。別のアカウントもクラウド化したい時は、また『クラウドオフロードをセットアップして』と言ってください。」 — `setup-cloud.sh` の終了メッセージにも同じ内容が出るので、それを要約して伝えればOK。

> 自動で窓を開けない・OSが分からない・上の方法でうまくいかない場合だけ、下の「手動でターミナルを開く方法」を案内する。

### 補足：双方向クリックランチャー（フォルダの中の `.command` / `.bat`）

このフォルダには、ダブルクリックするだけでターミナルが自動で開いて動く小さなファイルが入っている。ユーザーが「ターミナルって何？」となったら、これを使ってもらうのが一番ラク：
- `01_setup_mac.command`（Mac）/ `01_setup_windows.bat`（Windows） … `setup.sh` を実行
- `02_start_mac.command` / `02_start_windows.bat` … `start.sh` を実行（アプリを起動）
- `03_update_mac.command` / `03_update_windows.bat` … `update.sh` を実行（新版へ更新）
- `04_cloud_setup_mac.command` / `04_cloud_setup_windows.bat` … `setup-cloud.sh` を実行

※ Mac で初めてダブルクリックすると「開発元を検証できないため開けません」と出ることがある → その `.command` ファイルを **右クリック →「開く」→「開く」** を1回押せばOK（2回目以降は普通にダブルクリックで動く）。あなた（Claude Code）が代わりに開く時は、先に `xattr -dr com.apple.quarantine . 2>/dev/null` を実行しておくとこの警告が出ない。

### 手動でターミナルを開く方法（上の自動オープン／ランチャーが使えない時のフォールバック）

**■ Macの場合**
1. 画面下のバーにある「Finder」（青と白のニコちゃんマークのアイコン）を開く
2. このツールのフォルダ（`nuko-threads-webapp-v1` のような名前。zipを展開してできたフォルダ）を探す
3. そのフォルダを **右クリック**（マウスがなければ「control」キーを押しながらクリック）
4. メニューの下の方の「**サービス**」→「**フォルダに新規ターミナル**」をクリック
   - 「サービス」や「フォルダに新規ターミナル」が見当たらない時：画面右上の🔍（または Command＋スペース）で「**ターミナル**」と打って Enter → 開いた窓に `cd ` と打って **半角スペースを1つ空けてから、さっきのフォルダをその窓にドラッグ&ドロップ** → Enter
5. 黒っぽい（または白い）文字だけの窓が開く。それが「ターミナル」
6. こちらが渡すコマンドを **コピーして、その窓に貼り付け**（貼り付けは Command＋V）→ **Enter（return）キー** を押す

**■ Windowsの場合**
1. 「エクスプローラー」（黄色いフォルダのアイコン）を開く
2. このツールのフォルダ（`nuko-threads-webapp-v1` のような名前）を **ダブルクリックして“中に入る”**（フォルダの中身が一覧表示されている状態にする）
3. ウィンドウ上部の **アドレスバー**（「PC > デスクトップ > … 」のように今いる場所が表示されている横長の部分）を1回クリックすると文字を打てる状態になる
4. そこに **`cmd`** と打って Enter → 黒い窓「コマンドプロンプト」が、そのフォルダの場所で開く
   - ※「Git Bash」を入れている人は、フォルダ内の何もないところで右クリック →「Git Bash Here」でもOK（`bash 〜.sh` のコマンドはこちらの方が確実に動く）
5. 黒い窓に、こちらが渡すコマンドを **貼り付け**（貼り付けは「右クリック」または Ctrl＋V）→ **Enter** を押す

> どちらの場合も、ユーザーが「開けない」「どこにフォルダがあるか分からない」と言ったら、スクショを送ってもらって一緒に探す。**コマンドを渡して終わり、にしない。**

---

## 🎯 ユーザーの依頼に応じた行動

ユーザーが日本語で曖昧な指示を出してくることが多いので、以下のキーワードで判定して対応すること。

### 「セットアップして」「初期セットアップして」「インストールして」

⛔ **最重要**：ユーザーが「セットアップして」と言っても、すでに前のバージョンを使っていた可能性がある。いきなり `setup.sh` / `npm install ... migrate ...` を実行しない。必ず先に旧フォルダと旧 `prisma/dev.db` を探す。旧データが見つかったら、それは新規セットアップではなくアップデートとして扱い、`update.sh "<旧フォルダ>"` で引き継ぐ。

→ 上の **「📦 セットアップ／アップデートの統一フロー」** に従う。**まず前のバージョンが他に無いか自分で探し**、あれば実は「アップデート」（旧 `prisma/dev.db` を引き継ぐ）として進める。無ければ新規セットアップ：
```bash
npm install --no-audit --no-fund && npx prisma generate && npx prisma migrate deploy && npx tsx prisma/ensure-default-knowledge.ts
```
（依存インストール / Prismaクライアント生成 / DBマイグレーション / ナレッジ6本投入）→ **バックグラウンドで** `bash start.sh` 起動 → 「ブラウザで http://localhost:3000 を開いてください。最初に『設定 → アカウント追加』からアカウントを登録してください」。
※ ユーザーが「自分でターミナルでやりたい」と明言した場合だけ `bash setup.sh` か `初回セットアップ.command`（Mac）/ `.bat`（Windows）のダブルクリックを案内。それ以外は案内しない（あなたが代行する）。

### 「立ち上げて」「起動して」「Webアプリを開いて」「アプリを起動」「サーバー起動」

→ **あなたが バックグラウンドで** `bash start.sh` を実行（フォアグラウンドだとブロックする）。まず確認：
```bash
curl --max-time 5 -s -o /dev/null -w "%{http_code}" http://localhost:3000/api/accounts
```
200 が返れば起動済み（何もしない）。000/タイムアウトなら未応答なので `bash start.sh` をバックグラウンドで起動 → 起動後「ブラウザで http://localhost:3000 を開いてください」と案内（自動で開かなければ）。`Ready` 表示後もロード中のままなら、PCのメモリ不足/高負荷が濃厚なので、アプリ修正ではなく他アプリ終了・Mac再起動を案内する。
※ ユーザーが自分で起動したい場合は、フォルダ内の `アプリ起動.command`（Mac）/ `アプリ起動.bat`（Windows）のダブルクリックを案内（ターミナルが自動で開いて起動する）。

### 「クラウドオフロードをセットアップして」「PCを閉じても投稿できるようにして」

→ **上の「🖥️ コマンド実行 原則②」のとおり、ターミナル窓をあなたが自動で開いて `setup-cloud.sh` を走らせる**（「ターミナルを開いてこれを実行してください」と丸投げしない）。Mac は `osascript ... do script "cd ... && bash setup-cloud.sh"` で新窓ポップアップ。Windows は `クラウドオフロード設定.bat` のダブルクリックを案内。出てくる手動部分（Googleログイン / Apps Scriptで「デプロイ→新しいデプロイ→ウェブアプリ」「アクセス＝全員」→URLコピー→ターミナルに貼付）を1つずつ案内し、URL貼付まで付き合う。

### ⛔ クラウドオフロードのセットアップで、絶対にやってはいけないこと

このセクションだけは強く守ること。以下を破ると **「GAS コードが入っていない空デプロイ」「タイムゾーンが NY のまま」など、ユーザーが何時間もハマる事故の原因**になる（実際に多発した）。

- ❌ **`clasp` コマンドを自分で叩かない**（`clasp create` / `clasp push` / `clasp deploy` 等を直接実行しない）。クラスプの呼び出し順序・`appsscript.json` のコピータイミング・`rootDir` の書き換えは `setup-cloud.sh` が一括で正しい順序で実行する。順序を1つ間違えると Google 側のデフォルト manifest（NY タイムゾーン）が上書きで残り、投稿が一切飛ばなくなる。
- ❌ **GAS（Apps Script）のコードを自分で書かない・貼り付けない・生成しない**。`gas/appscript.gs` がこのリポジトリの正本。これを `clasp push --force` で送る以外の経路を作らない。Apps Script エディタに直接コードを書く案内もしない。
- ❌ **GAS プロジェクトを手動で作らない**（Google Drive で「新規 → Apps Script」「新規 → スプレッドシート → 拡張機能 → Apps Script」みたいな手順を案内しない）。`setup-cloud.sh` が `clasp create --type sheets` で正しく作る。
- ❌ **`gas-deploy/<accountId>/` の中をいじらない**（`appsscript.json` を編集する／`.clasp.json` を書き換える／`appscript.gs` を直接編集して push する、等）。リポジトリ本体（`gas/`）を直してそこから `setup-cloud.sh` で流し直す以外の経路は禁止。
- ❌ **「クラウドオフロード設定がうまくいかない」と言われても、自分で clasp や GAS を触り始めない。** まず `bash setup-cloud.sh` を実行 → 出てきたエラーメッセージを読む → そこから判断。それでも分からなければユーザーに「`setup-cloud.sh` の出力を全部貼ってください」と頼む。

正しい呼び出しは **必ず `bash setup-cloud.sh` を走らせる** か、もしくはユーザーに `クラウドオフロード設定.command` / `.bat` のダブルクリックを案内する、この2択だけ。

※ `setup-cloud.sh` は JSON 操作・組み立てを `node -e` で行う（以前は `python3` だったが、Windows 含めて Python 環境差でハマる人がいたため）。Node.js は `[1/8]` で必須チェック済みなので、追加で `python3` の存在チェックは不要。

※ **既にそのアカウントでクラウドオフロード設定済みの場合**、`setup-cloud.sh` は「① Google投稿を修復する ／ ② 最初からやり直す」と聞いてくる。**アップデート後は ①** を選ばせる。アプリ側の修復APIが、コードPush、新しいWeb Appデプロイ、動作するURLの保存、自動実行・タイムゾーン・投稿用トークン・予約キューの確認まで行う。`②` は普段使わない（スプシを作り直したい特殊ケースのみ）。

**前提（満たしていなければ先に案内）:**
1. Webアプリが起動している（`setup-cloud.sh` 内で自動起動も対応）
2. 対象アカウントの Threads アクセストークンが保存済み（未設定なら「設定→アカウント編集→アクセストークン」で先に保存）

仕組み: PC を閉じても予約投稿を動かすため、Googleスプレッドシート＋GAS時間トリガーで投稿実行する。`setup-cloud.sh` が clasp自動インストール / Googleログイン誘導 / アカウント選択 / スプシ作成＋GASコードPush / Apps Scriptエディタ自動オープン まで自動。**手動はGASエディタでのデプロイとURLのコピペだけ**。その後（認証キー生成・GAS Properties設定・Web DB保存・有効化・動作確認）は全自動。

仕様メモ：GAS プロジェクトのタイムゾーンは `gas/appsscript.json` の `"timeZone": "Asia/Tokyo"` で固定されており、`setup-cloud.sh` の Step 4 で `clasp create` の**後**に `appsscript.json` を再コピーしてから `clasp push --force` する（clasp create がローカルの manifest をユーザーの Google アカウントのデフォルト＝NY 等で上書きするため、後コピーが必須）。Asia/Tokyo 以外だと `processScheduledPosts` が安全のため投稿を停止する（GAS 側の防御）。setConfig のレスポンスに `scriptTimeZone` が含まれており、webapp の `cloud/setup initialize` が Asia/Tokyo 以外を弾いて 422 を返す。`setup-cloud.sh` 側でも Step 6 で同様にチェックして中止する（二重防御）。

### 「クラウドオフロードを無効化したい」「OFFにして」

→ Webアプリの「設定→アカウント編集→☁ クラウドオフロード」セクションで「無効化」ボタンを押すよう案内。
GAS側に queued が残っている場合は「transferBack（Web側に引き戻し、推奨）」or「force（強制無効化）」を選ぶ。

### 「投稿を生成して」「投稿作って」「○本生成して」

→ Webアプリの画面操作で行う：
1. ブラウザで `http://localhost:3000` を開く（必要なら start.sh で起動）
2. 右上「📝 投稿」→「AI生成」ボタン
3. アカウントと生成数を選んで「生成開始」

直接APIを叩く必要はない。UIで完結する。

### 「Threadsトークンを取得して」「APIトークン作って」

→ **Proプランの方は手動取得を推奨**（Claude Codeで自動取得するとProの利用枠を大量消費するため）。

手動の場合は GitMind を案内：
https://gitmind.com/app/docs/m0456v7u
（「2. Threads APIアクセストークン取得」の章だけ読み進めるよう案内）

ユーザーが「自動でやって」と希望した場合のみブラウザ操作で取得。

### 「コンセプトを設定したい」「コンセプトシートを書きたい」

→ Webアプリの「設定→アカウント編集→コンセプトシート」で書く。「💡記入テンプレを挿入」ボタンでテンプレが入るので、それを埋めるよう案内。

### 「Threadsトークンの期限が切れた」「投稿でエラー出る」

→ 通常運用：Meta開発者ポータルで再発行 → Webアプリの「設定→アカウント編集→アクセストークン」を更新。
クラウドオフロード使用中なら GAS側で毎日チェックし、通常は20日ごと（失敗時は毎日リトライ）に自動リフレッシュされるので、通常は期限切れにならない。

### 「トークンが取得できない」「ログインまで進めない」「テストユーザー追加が保存できない」

これは**ツール側の不具合ではなく、Meta（Facebook / Threads）側の操作の問題**であることがほとんど。コードを修正しようとしないこと。以下を順に案内する：

1. **「アプリにアイコン画像が出ていない / テストユーザー追加を押しても保存されない」**
   → 入力欄に**アカウント名を打ち込んで候補から選択**する必要がある（クリックで確定）。アイコンが表示されていない＝まだ選択できていない状態。
   → さらに、**自動投稿したい Threads アカウントに、同じブラウザで別タブからログインしていない**ことを確認（ログインしていると候補に出ないことがある）。**シークレット/プライベートウィンドウ**で作業するのが確実。

2. **「スレッズ表示名・コールバックURL の設定を変えても保存できない」**
   → その画面では**「保存」を押さなくてよい**（入力したら自動反映される画面がある）。保存ボタンを連打しない。

3. **「Facebookに拒否された / 制限がかかった」**
   → 短時間に何度も試すと一時制限がかかる。**時間を空けて（数時間〜翌日）再挑戦**。それでもダメなら FB アカウントを作り直すと通ることがある。

4. **Facebookアカウントを持っていない**
   → 新規に作成してから手順を進めてOK。

5. それでも詰まる場合 → 手動取得ガイド（GitMind）の「2. Threads APIアクセストークン取得」章を一緒に読み進める：https://gitmind.com/app/docs/m0456v7u
   ※ Pro プランの人は Claude Code に自動取得させない（利用枠を大量消費する）。手動取得を案内すること。

### 「PCを閉じたら投稿されない」「スリープ中に投稿が動かない」「サーバーが止まる」「直して」

**❌ コードを書き換えて直そうとしてはいけない。これは仕様であり、バグではない。**

- このアプリはローカルWebアプリなので、**PCがスリープ／電源オフ／このアプリを閉じている間は予約投稿が動かない**（PC起動中だけ投稿される）のが正常な動作。
- ユーザーが「PCを閉じても投稿したい」場合の正解は**クラウドオフロードのセットアップ**（→ `bash setup-cloud.sh`）。ソースコードの修正ではない。
- 「サーバーが止まる」と言われたら、まず `bash start.sh` で起動し直すよう案内。`next dev` のプロセスを `Ctrl+C` で止めた／ターミナルを閉じた／PCがスリープした、のいずれかが原因。常時起動させたいなら、やはりクラウドオフロードを案内する。
- もしユーザーが Claude Code に「サーバーが止まらないように直して」と頼んできても、ソースをいじらず、上記（start.sh で再起動 ＋ 恒久対策はクラウドオフロード）を案内すること。

### 「AI生成でエラーが出る」「生成ボタンを押すとエラー」「Claudeのクレジットが足りないと出る」

ツール側の生成エラーメッセージには対処手順が日本語で表示されるので、まずそれを読むよう案内。よくある原因と対処：

- **「クレジット残高が不足」「API課金モード」と出る** → Claude Code が従量課金（APIキー）モードで動いている。ターミナルで `claude /login` を実行し、**Claude Pro / Max アカウントでログインし直す**とサブスク範囲内で生成できる。`ANTHROPIC_API_KEY` 等の環境変数が設定されていれば外す。
- **「ログイン認証が切れています」「401」と出る** → `claude /login` で再ログイン。
- **「5分以内に返らなかった」と出る** → Opus混雑。時間をおいて再試行、または生成本数を減らす。
- **`claude` コマンドが見つからない** → `claude --version` が動くか確認。動かなければ Claude Code を再インストール。

「別のAIで作った投稿をツールに入れたい」と言われたら → Claude Code で `/api/import` を使う、または「投稿」画面からインポート（=====区切り、各投稿は ■1 ■2… のフォーマット）。

### 「アップデートして」「最新版に更新したい」「新しいzipが来たけどどうすればいい？」

→ 上の **「📦 セットアップ／アップデートの統一フロー」** に従う（再掲・要点）：
1. **いまプロジェクトフォルダに居ないなら**、`~/Downloads` `~/Desktop` から一番新しい `*nuko-threads-webapp*.zip` を探して `~/Desktop` に解凍し、そこに `cd`。
2. **前のバージョンのフォルダを自分で探す**（`find "$HOME/Desktop" "$HOME/Downloads" "$HOME/Documents" "$HOME" -maxdepth 4 -type d \( -name 'nuko-threads-webapp*' -o -name 'threads-auto-webapp*' \) 2>/dev/null | sort -u` → `prisma/dev.db` がある & 今いる新フォルダ自身でない もの）。**ユーザーにパスを聞かない。** 複数あれば plain な選択肢で「どっちで運用してた？」と聞く（場所＋アカウント数を添える）。
3. `printf 'n\n' | bash update.sh "<見つけた旧フォルダ>"` を実行 → 旧 `prisma/dev.db`（アカウント・コンセプト・投稿・キュー・クラウドオフロード設定）を新フォルダに引き継ぎ＋`npm install`＋`prisma generate`＋`prisma migrate deploy`（既定ナレッジの seed は再実行しない＝編集を上書きしないため）。
4. `bash start.sh` をバックグラウンド起動 → 「アップデート完了。今までのアカウント・コンセプトがそのまま画面に出ているはずです。ブラウザで http://localhost:3000 を開いてください。**前のフォルダはすぐ消さず、ちゃんと動くのを確認してから消してください**」と plain に伝える。
5. クラウドオフロード設定済みなら続けて `setup-cloud.sh` →「① Google投稿を修復する」（コード・自動実行・動作するURL・予約キューまで確認）。
6. 前のバージョンが**見つからなかった**＝引き継ぐデータなし＝**新規セットアップ**（`npm install … && npx prisma generate && npx prisma migrate deploy && npx tsx prisma/ensure-default-knowledge.ts` → `bash start.sh`）。

⚠️ やってはいけないこと:
- ❌ 古いソースファイルだけを手で書き換える（差分パッチ運用）— **フォルダごと置き換える**のが安全
- ❌ `prisma/dev.db` を削除する／別の dev.db で上書きする — ユーザーの全データが消える
- ❌ ユーザーに「前のフォルダのパスを教えて／貼り付けて」と聞く — 自分で `find` して見つける
- ❌ `.command`/`.bat` のダブルクリックや「ターミナルで〜して」を案内する — あなたが直接コマンドを実行して終わらせる（ユーザーが「自分でやりたい」と明言した時だけ案内）
- ❌ アップデートのつもりで（旧データがあるのに）`bash setup.sh` 相当を実行する — アカウント、アクセストークン、下書き、予約投稿が新フォルダに引き継がれず、ユーザーには「消えた」ように見える。旧データがあれば必ず `update.sh`

詳しい手順はフォルダ内の `UPDATE.md` 参照。

---

## ⚠️ Claude Code 自体の認証エラーが出たら

ユーザーが「生成」ボタンを押して「401 Invalid authentication credentials」のようなエラーが出る場合：

→ Claude Code CLI のログイン認証切れ。ターミナルで以下を実行するよう案内：

```bash
claude /login
```

ブラウザで再認証してから「生成」を再試行する。

---

## 🏗️ このアプリの設計上の重要ポイント

- **PCを閉じても投稿させたい場合だけ**クラウドオフロードを有効にする。Webアプリ単体（PC起動中のみ投稿）でも問題なく動く
- クラウドオフロードはアカウント単位で ON/OFF（複数アカウント運用時は別々に管理）
- 1スプシ＝1Threadsアカウントが原則
- 投稿実行の権威は `Post.executor` カラム（"local"=Web cron / "gas"=GASトリガー）で物理的に分離。**二重投稿は構造的に発生しない**
- トークン期限管理は GAS 側で毎日チェックし、通常は20日ごと（失敗時は毎日リトライ）に自動リフレッシュ
- 投稿失敗時は自動で3回リトライ（15分間隔）。3回失敗で status="error" 確定（DBには残る）
- **予約時刻を15分以上過ぎた投稿は、投稿せず自動でスキップ（status="error" にして残す）**。PCがスリープ/電源オフ/アプリ停止で長時間止まっていた後に復帰したとき、溜まっていた過去の予約が遅れて一気に投稿される事故を防ぐため。正常運用ではcronが1分以内に拾うのでこの閾値には当たらない。これに該当した投稿が出たら「PCがスリープ/停止していた可能性あり。常時動かしたいならクラウドオフロードを」と案内する（コードは触らない）

---

## 📁 ファイル構成

```
nuko-threads-webapp-v1/
├── README.md          ← ユーザー向け使い方
├── UPDATE.md          ← 旧版からのアップデート手順
├── 01_setup_mac.command / 01_setup_windows.bat         ← ★初回セットアップ
├── 02_start_mac.command / 02_start_windows.bat         ← ★アプリ起動
├── 03_update_mac.command / 03_update_windows.bat       ← ★新版へ更新
├── 04_cloud_setup_mac.command / 04_cloud_setup_windows.bat ← ★クラウドオフロード設定
├── setup.sh           ← 初回セットアップ本体（npm install + DB初期化）
├── update.sh          ← 旧版から設定を引き継いで更新する本体（旧 prisma/dev.db をコピー）
├── start.sh           ← 起動本体（npx next dev）
├── setup-cloud.sh     ← クラウドオフロード設定本体
├── src/               ← Next.js アプリ本体（このプロジェクト）
├── prisma/            ← DB スキーマ + マイグレーション + ナレッジ
├── gas/               ← Google Apps Script コード（クラウドオフロード用）
└── tests/             ← 統合テスト（npm run test:cloud で実行可）
```
（`.command` = Mac用ダブルクリック起動。`.bat` = Windows用。中身はそれぞれ対応する `.sh` を実行するだけ。）

---

## このNext.jsはあなたが知ってるNext.jsとは違う

破壊的変更含むバージョン。API・規約・ファイル構造が学習データと異なる可能性があるため、
コードを書く前に `node_modules/next/dist/docs/` の関連ガイドを読むこと。Deprecation警告に従うこと。

@AGENTS.md
