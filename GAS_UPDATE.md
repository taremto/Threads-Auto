# Google投稿を修復する手順

クラウドオフロードを使っている方だけ必要です。

アプリ本体をアップデートしても、Google側に置いてあるGASコードやWeb App URLは自動では更新されません。
「今すぐ同期」は投稿結果を取り込むだけで、Google側の修復ではありません。

## いちばん簡単な方法

1. アプリを開く
2. 「自動投稿チェック」で、対象アカウントの赤い項目を開く
3. `このアカウントのGoogle投稿を修復` を押す
4. 「Google投稿を修復しました」と出れば完了

または、設定画面のアカウント編集にあるクラウドオフロード欄で、`Google投稿を修復する` を押してください。

これで、Google側のコード・自動実行・タイムゾーン・投稿用トークン・予約キューをまとめて確認します。
古いWeb App URLが壊れている場合は、動作するURLをアプリ側へ自動で保存し直します。

## ボタンで失敗した場合

1. このアプリのフォルダを開く
2. 次のファイルを開く
   - Mac: `04_cloud_setup_mac.command`
   - Windows: `04_cloud_setup_windows.bat`
3. 黒い画面が開いたら、更新したいアカウントを選ぶ
4. 次の選択肢が出たら、`1` を入力して Enter

   ```text
   ① Google投稿を修復する
   ② 最初からやり直す
   ```

5. 「Google投稿を修復しました」と出たら完了
6. アプリの「自動投稿チェック」で状態を確認する

## 複数アカウントがある場合

クラウドオフロードを使っているアカウントごとに、同じ手順を繰り返してください。

アプリはアカウントごとに別々のGoogle連携を使います。別アカウントを修復しても、他のアカウントには反映されません。

## 途中で 1 の選択肢が出ない場合

そのアカウントは、まだクラウドオフロードが未設定か、古いフォルダからGAS作業情報が引き継がれていない可能性があります。

画面に出た内容をそのままサポートに送ってください。二重投稿を避けるため、分からない状態で「最初からやり直す」を連打しないでください。

## よくある勘違い

- Zipを入れ替えるだけでは、既存のGASは更新されません。
- 「今すぐ同期」は、GASの投稿結果をWebアプリに取り込む操作です。Google投稿の修復ではありません。
- スプレッドシートを直接編集しても、GASコードは更新されません。
- 修復後、必要な場合はWeb App URLが自動で新しいものに保存し直されます。ユーザーが手でURLを探す必要はありません。
