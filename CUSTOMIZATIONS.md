# ローカルカスタマイズ記録

このファイルは、製品アップデート時にローカル機能を再適用するための記録。
既存のカスタマイズ全体の監査は未実施で、このファイルは2026-06-27に追加した機能から記録を開始している。

## 1. 専門ナレッジの分離管理・URL取り込み

### 目的

- `07_専門ナレッジ` 由来の知識を通常ナレッジと混ぜず、専用タブで管理する。
- 記事URL、YouTube文字起こし、貼り付け本文から、新しい専門ナレッジの下書きを作る。
- AI変換結果を自動保存せず、ユーザーが確認・編集してから保存できるようにする。
- 投稿生成では通常の追加ナレッジと別枠にし、「1投稿＝1カテゴリ」の参照素材として扱う。

### 変更箇所

- `src/components/SettingsPage.tsx`
  - システム設定に「専門ナレッジ」タブを追加。
  - タイトルが `専門ナレッジ` で始まる `custom` ナレッジを専用タブへ分離。
  - URL、本文・文字起こし、対象アカウントの入力欄を追加。
  - 生成AIを「自動切替（Claude→Codex）」「Codex」「Claude」から選択可能。
  - AI変換後は通常の新規作成フォームへ下書きとして渡し、「保存」を押すまでDBへ追加しない。
- `src/app/api/knowledge/from-url/route.ts`
  - 公開Web記事の本文取得と、貼り付けテキストのフォールバックに対応。
  - YouTube、PDF、ログイン必須ページ、取得失敗ページは本文・文字起こし貼り付けを案内。
  - ローカルネットワーク宛てURLを拒否し、取得サイズ・時間・転送回数を制限。
  - ClaudeまたはCodex CLIで01〜06の1カテゴリに整理し、出典URL入りのMarkdown下書きを返す。
  - 自動切替ではClaudeが利用制限・エラーの場合にCodexへフォールバックする。
- `src/lib/codex-cli.ts`
  - Codex CLIを非対話・一時セッション・読み取り専用で実行。
  - ChatGPTログインを強制し、APIキー環境変数を子プロセスへ渡さない。
  - 不要なプラグイン、ブラウザ、コンピューター操作などを無効化。
- `src/app/api/generate/route.ts`
  - `専門ナレッジ` で始まる `custom` ナレッジを通常ナレッジから分離。
  - 専門ナレッジ専用プロンプトで「1投稿＝1カテゴリ」「action系の重複回避」を指示。
- `prisma/dev.db`
  - `専門ナレッジ 00〜99` の10件を、らいとアカウント固有ナレッジとして保持。
  - 01〜07（05Aを含む）は生成に使用。00と99は管理・出典用のため生成では未使用。

### 使い方

1. システム設定 → 専門ナレッジを開く。
2. 生成AIを選ぶ。迷ったら「自動切替（Claude→Codex）」を使う。
3. 記事はURLを入力して変換する。
4. YouTubeなどURLだけで取得できない場合は、URLに加えて文字起こし・本文を貼る。
5. 変換後のタイトル・本文・対象アカウントを確認し、「保存」を押す。

### 再適用時に確認すること

- [ ] `Knowledge.enabled` が存在し、生成APIが `enabled: true` だけを読む。
- [ ] 生成APIがタイトル接頭辞 `専門ナレッジ` で通常ナレッジと分離する。
- [ ] 設定画面に「専門ナレッジ」タブがあり、通常タブと重複表示されない。
- [ ] URL変換結果が自動保存されず、編集可能な新規作成フォームへ入る。
- [ ] 生成AIの選択肢に「自動切替」「Codex」「Claude」がある。
- [ ] Codex CLIがChatGPTログイン・読み取り専用・一時セッションで実行される。
- [ ] 自動切替でClaude失敗時にCodexへフォールバックする。
- [ ] YouTube URLだけの場合に文字起こし貼り付け案内が出る。
- [ ] 型チェックと対象ファイルのESLintを実行する。

## 2. AI投稿生成のClaude・Codex切り替え

### 目的

- 通常のAI投稿生成でもCodexを選べるようにする。
- Claudeが利用制限・エラーのとき、投稿ルールや保存処理を変えずにCodexへ切り替える。
- Claude専用の使用量制限でCodex生成まで止まらないようにする。

### 変更箇所

- `src/components/GenerateModal.tsx`
  - AI投稿生成画面に「自動切替（Claude→Codex）」「Codex」「Claude」の選択欄を追加。
  - Codex選択時はClaudeの準備状態・使用量で生成ボタンや投稿数を制限しない。
  - 完了時に実際に使ったAIと、自動切替の有無を表示。
- `src/app/api/generate/route.ts`
  - `provider` を受け取り、ClaudeまたはCodex CLIで同じ投稿生成プロンプトを実行。
  - 自動切替ではClaude使用量が上限ならCodexへ直行し、実行エラー時もCodexへフォールバック。
  - Claude出力の形式を解析できない場合も、Codexで1回だけ再生成する。
  - 重複回避、専門ナレッジ、推奨時間、DB保存処理は既存のまま共通利用。
- `src/lib/codex-cli.ts`
  - 専門ナレッジ変換と投稿生成で共通利用。

### 使い方

1. ダッシュボードの「AI生成」を開く。
2. 「生成AI」を選ぶ。迷ったら「自動切替（Claude→Codex）」。
3. 投稿数・追加指示を設定して「生成開始」を押す。

### 再適用時に確認すること

- [ ] AI投稿生成画面に3つの生成AI選択肢がある。
- [ ] Codex選択時にClaudeの制限で生成ボタンが無効にならない。
- [ ] `/api/generate` が `provider` を受け取り、Codex CLIでも同じプロンプトを使う。
- [ ] 自動切替時にレスポンスの `providerUsed` と `fallbackFrom` が画面表示へ反映される。
- [ ] 既存の類似チェック・推奨時間・DB保存形式が変わっていない。

## 3. ダークテーマのプルダウン可読性

### 目的

- Windowsでプルダウンを開いたときの「白背景＋白文字」を防ぎ、選択肢を読みやすくする。

### 変更箇所

- `src/app/globals.css`
  - `select option` に濃い背景色と明るい文字色を明示。
  - 選択中の項目は水色背景と濃い文字色にしてコントラストを確保。

### 再適用時に確認すること

- [ ] 「生成AI」などのプルダウンを開き、すべての選択肢が読める。
- [ ] 選択中と未選択の項目を見分けられる。

## 4. Claude Code投稿生成フローの可視化・ナレッジ整理

### 目的

- WebUIの一括AI生成とは別に、`post-generation` スキル（STEP0〜14）の進行状況を表示する。
- 現在のSTEP、実際に参照しているナレッジ、各STEPの要約・出力をリアルタイムで確認できるようにする。
- ナレッジ一覧を優先軸と使用STEPで整理し、参照順を把握しやすくする。

### 変更箇所

- `prisma/schema.prisma`
  - `GenerationSession` と `GenerationStep` を追加。
- `prisma/migrations/20260628004549_add_generation_sessions/migration.sql`
  - 生成セッションとSTEP履歴を保存するテーブル・インデックスを追加。
- `src/app/api/generation-flow/`
  - `progress` でClaude CodeからのSTEP通知を保存。
  - `active` と `sessions` で進行中セッション・履歴を返す。
  - STEP13.5は内部値135だが、セッション完了はSTEP14完了時だけにする。
- `src/lib/generation-steps.ts`
  - STEP0〜14の表示名と標準参照ナレッジを定義。
- `src/lib/knowledge-metadata.ts`
  - ナレッジタイトルを6つの優先軸、使用STEP、軸内優先順へ対応づける。
- `src/components/GenerationFlowPage.tsx`
  - 縦型ステッパー、現在STEP、参照ナレッジ、詳細出力、セッション履歴を表示。
  - 進行中セッションをポーリングしつつ、ユーザーが選んだ過去履歴は勝手に切り替えない。
- `src/components/Sidebar.tsx` / `src/components/Dashboard.tsx`
  - 「生成フロー」タブを追加。
- `src/components/SettingsPage.tsx`
  - ナレッジを「優先軸グループ」と「フラット」で切り替え。
  - 各ナレッジに使用STEPバッジを表示し、グループ内を優先順で表示。
- `.claude/skills/post-generation/SKILL.md`
  - 各STEP開始・完了時のローカルWebUI向け進捗通知ルールを追加。

### 再適用時に確認すること

- [ ] 既存の `prisma/dev.db` をリセットせず、生成フロー用マイグレーションだけを適用する。
- [ ] Prisma Clientを再生成する。
- [ ] サイドバーから「生成フロー」を開ける。
- [ ] STEPのrunning/completed/skipped/failedが正しい色と記号で表示される。
- [ ] STEP13.5完了後もセッションが進行中で、STEP14完了時だけ完了になる。
- [ ] 進行中セッションがあっても、選択した過去セッションを閲覧し続けられる。
- [ ] ナレッジの優先軸グループ、軸内順序、STEPバッジが表示される。
- [ ] WebUI停止中でも進捗通知失敗が投稿生成を止めない。
- [ ] 型チェックと対象ファイルのESLintを実行する。

## 5. WebUI起点のCodex投稿生成・承認後キュー追加

### 目的

- アカウント名をクリックして下書き画面へ移動し、画面の「生成開始」からCodex投稿生成フローを実行する。
- STEP0〜13の進行状況と参照ナレッジを生成中に確認する。
- 生成結果をDBへ即保存せず、ユーザーが最終稿と予約日時を確認して承認してからキューへ追加する。
- 承認後の予約処理は既存の下書きと同じキューAPIを使い、ローカル投稿・クラウドオフロード・重複時間チェックを共通化する。

### 変更箇所

- `src/components/Dashboard.tsx`
  - アカウント名クリック時に投稿ページの下書きタブへ移動。
  - 下書き画面に「生成開始」を追加し、従来の一括生成は「通常AI生成」として併存。
- `src/components/CodexFlowModal.tsx`
  - Layer、悩み・テーマ、追加指示を入力してCodex生成を開始。
  - 生成中のSTEP・参照ナレッジ・進行状況を表示。
  - STEP13完了後に最終稿を編集・確認し、予約日時を選んで承認。
  - 承認後は `/api/posts/group-action` の `queue` を呼び、既存の日時指定・クラウド自動投稿フローを利用。
- `src/app/api/generation-flow/start/route.ts`
  - WebUI操作から生成セッションを作成。
- `src/app/api/generation-flow/approve/route.ts`
  - 承認済み最終稿を初めて下書きとして保存。
  - 同じセッションの再試行では既存下書きを再利用し、二重作成を防止。
- `src/app/api/generate/route.ts`
  - `previewOnly` と `flowSessionId` に対応。
  - Codex生成中にSTEP進捗を更新し、承認前は投稿テーブルへ保存しない。
- `src/components/GenerationFlowPage.tsx`
  - 「承認待ち」「キュー追加中」のセッション状態を日本語表示。

### 再適用時に確認すること

- [ ] アカウント名をクリックすると下書きタブへ移動する。
- [ ] 「生成開始」と従来の「通常AI生成」が併存する。
- [ ] 承認前に `Post` レコードが作成されない。
- [ ] 生成中に現在STEPと参照ナレッジが更新される。
- [ ] 最終稿を編集してから承認できる。
- [ ] 予約日時が過去の場合はキュー追加を止める。
- [ ] 承認後のキュー追加が `/api/posts/group-action` を通り、クラウドオフロードON時はGASへ送られる。
- [ ] キュー追加失敗後の再試行で同じ下書きが再利用され、重複作成されない。
- [ ] 型チェック、対象ファイルのESLint、本番ビルドを実行する。

## 6. 投稿生成フローの複数生成・AI選択・投稿別メタ表示

### 目的

- 通常AI生成と同じように投稿文生成数を指定する。
- 投稿ごとにL1・L2・L3をユーザーが指定する。
- 指定数すべてで、具体場面・感情・結論・フック型・構造・アクションが異なる切り口を使う。
- ClaudeとCodexのどちらでも同じ生成・承認・キュー追加フローを使う。
- 生成後に、各投稿が使った切り口・フック型・構造・参照ナレッジを確認する。

### 変更箇所

- `src/components/CodexFlowModal.tsx`
  - 生成AI、投稿文生成数、投稿別Layerの選択欄を追加。
  - 投稿ごとに本文・予約日時を編集可能。
  - 切り口、フック型、構造、参照ナレッジを投稿カードへ表示。
  - 複数投稿を順番に既存キューAPIへ渡し、途中失敗時は残りだけ再試行。
- `src/app/api/generation-flow/start/route.ts`
  - 投稿別Layer配列を生成セッションへ保存。
- `src/app/api/generate/route.ts`
  - 生成AIが返す `FLOW_META` 行を本文から分離して投稿別メタ情報として返却。
  - 既存の類似チェックで指定数を満たせない場合は承認へ進めず、別切り口での再生成を案内。
  - 生成フロー履歴のSTEP13出力に投稿別メタ情報を保存。
- `src/app/api/generation-flow/approve/route.ts`
  - 複数ツリーを別groupとして一括下書き化。
  - 再試行時は作成済みの複数groupを再利用。
- `src/components/Dashboard.tsx`
  - アカウントの投稿時間設定を生成フローモーダルへ渡し、通常AI生成と同じ生成数初期値を使う。

### 再適用時に確認すること

- [ ] 生成AIをClaude／Codexから選択できる。
- [ ] 投稿文生成数の初期値がアカウントの1日投稿設定と一致する。
- [ ] 生成数を変えると投稿別Layer欄も同じ数へ増減する。
- [ ] 全投稿でL1・L2・L3を個別指定できる。
- [ ] 指定数より少ない、または似た切り口が混ざった生成結果は承認へ進まない。
- [ ] `FLOW_META` 行が投稿本文・下書きへ混入しない。
- [ ] 生成後に切り口・フック・構造・参照ナレッジが投稿別に表示される。
- [ ] 複数投稿が別groupとして保存され、別々の予約日時でキューへ追加される。

## 7. 起動済みWebUIのPrisma DLLロック回避

### 目的

- WindowsでWebUIがすでに動いている状態から起動ファイルをもう一度開いても、Prisma ClientのDLL再生成で `EPERM` エラーにしない。
- 起動済みの画面をそのまま開き、保存DB・アカウント・投稿キューを変更しない。

### 変更箇所

- `start.sh`
  - `ensure_database` より前に `http://localhost:3000/api/accounts` の応答を確認する。
  - HTTP 200ならPrismaのマイグレーション・Client再生成を行わず、既存WebUIを開いて正常終了する。
  - WebUIが動いていない場合だけ従来どおりDB準備とPrisma Client生成を行う。

### 再適用時に確認すること

- [ ] WebUI起動中に起動ファイルを再実行しても `query_engine-windows.dll.node` の `EPERM` が出ない。
- [ ] 起動済みの場合はPrisma処理を飛ばして既存WebUIを開く。
- [ ] WebUI停止中は従来どおりDB準備後に新規起動する。

## 8. アカウント別ペルソナ設計

### 目的

- 発信者・テーマ・提供価値・トーンを定義する「アカウントコンセプト」と、投稿を届ける読者の状況・悩み・感情・反論・望む未来を分けて管理する。
- ペルソナ設計を通常AI生成、生成フロー、下書きツリー拡張のプロンプトへ反映する。
- 既存アカウントはペルソナ未入力のままでも従来どおり利用できるようにする。

### 変更箇所

- `prisma/schema.prisma`
  - `Account.personaSheet` を任意項目として追加。
- `prisma/migrations/20260628134000_add_account_persona_sheet/migration.sql`
  - 既存データを保持したまま `personaSheet` 列を追加。
- `src/lib/persona-template.ts`
  - 読者の具体的な場面・三層の悩み・本人の言葉・反論・望む未来を整理する記入テンプレを追加。
- `src/components/SettingsPage.tsx`
  - アカウント編集に「アカウントコンセプト」と独立した「ペルソナ設計」欄を追加。
  - 両方でテンプレ挿入・AI編集・保存状態表示を利用可能にする。
- `src/app/api/accounts/update/route.ts`
  - `personaSheet` の保存に対応。
- `src/app/api/accounts/concept-ai-edit/route.ts`
  - `sheetType` によりアカウントコンセプト／ペルソナ設計をそれぞれの目的でAI編集。
- `src/app/api/generate/route.ts`
  - 投稿生成プロンプトへペルソナ設計を独立セクションとして追加。
- `src/lib/extend-thread-prompt.ts` / `src/app/api/posts/extend-thread/route.ts`
  - 下書きツリーの追加・作り直しにもペルソナ設計を反映。
- `src/app/api/generation-flow/approve/route.ts`
  - 推奨投稿時間の読者判定でコンセプトとペルソナを併用。

### 再適用時に確認すること

- [ ] 既存の `prisma/dev.db` をリセットせずマイグレーションだけを適用する。
- [ ] アカウント編集に「アカウントコンセプト」と「ペルソナ設計」が別々に表示される。
- [ ] ペルソナ設計だけを編集・保存してもアカウントコンセプトが変わらない。
- [ ] ペルソナ設計で記入テンプレとAI編集を利用できる。
- [ ] ペルソナ未設定の既存アカウントでも投稿生成を続けられる。
- [ ] 通常生成・生成フロー・ツリー拡張のプロンプトにペルソナ設計が含まれる。

## 9. 外部編集したMarkdownナレッジの更新ボタン

### 目的

- VS Code、Claude、Codexなど別のソフトで `knowledge/` 配下のMarkdownを編集したあと、ターミナル操作なしでWebアプリのDBへ反映する。
- 反映前に差分件数を確認し、意図しないDB更新を防ぐ。
- 既存ナレッジのタイトル・本文変更と、新しく追加されたMarkdownの両方を反映する。削除は行わない。

### 変更箇所

- `src/app/api/knowledge/sync/route.ts`
  - `knowledge/` 配下のMarkdownを再帰的に読み込み、フロントマターの `id` で既存DBナレッジと照合。
  - IDが空の場合は同じアカウント・同じタイトルで既存DBを照合し、本文変更を検出。
  - 既存行に一致しないMarkdownは、`type`・`accountId`・`enabled`を引き継いで新規ナレッジとして追加。
  - `apply: false` で更新・追加件数を確認し、`apply: true` でトランザクション反映。
  - README、フロントマターなし、タイトルなし、重複ID、曖昧な同名、存在しないアカウントは反映せず集計だけ返す。
  - WindowsのCRLF・UTF-8 BOMに対応し、ファイル数とサイズに上限を設ける。
- `src/components/SettingsPage.tsx`
  - 通常ナレッジ・専門ナレッジの上部に「ナレッジ更新」ボタンを追加。
  - 差分がある場合はページ内に更新件数・追加件数・タイトル・「反映／キャンセル」を表示してから反映。
  - 更新後に一覧を再取得し、更新数・追加数・変更なし・対象外件数を表示。
  - 編集中・AI処理中は更新ボタンを無効化。
- `knowledge/README.md`
  - 外部編集後は「設定 → ナレッジ → ナレッジ更新」から差分確認・反映する手順へ更新。

### 再適用時に確認すること

- [ ] ナレッジ画面と専門ナレッジ画面に「ナレッジ更新」が表示される。
- [ ] ボタンの最初の処理ではDBを書き換えず差分だけを確認する。
- [ ] ページ内の確認でキャンセルした場合はDBが変更されない。
- [ ] 確認後は同じID、またはIDなしで同じアカウント・同じタイトルの本文変更が更新される。
- [ ] 新しいMarkdownは確認後にDBへ追加される。
- [ ] DB行の削除は起きず、既存行のenabled/type/accountIdは変更されない。
- [ ] フロントマターなし・タイトルなし・曖昧な同名はエラー終了せず「対象外」として表示される。

## 10. 生成結果の自動下書き保存・AI修正指示のナレッジ候補化

### 目的

- 投稿生成フローが完了した時点で、生成結果を自動的に下書きへ保存する。
- 下書きの本文を自然文の指示で繰り返しAI修正できるようにする。
- 実際に採用して保存したAI修正指示だけをナレッジ反映候補として残す。
- 候補は自動反映せず、ユーザーが一件ずつ内容と反映先を確認して適用または見送れるようにする。

### 変更箇所

- `src/components/CodexFlowModal.tsx`
  - 生成完了後は自動承認せず、「生成フローで1投稿ずつ確認」からSTEP13.5へ移動。
  - 投稿ごとの承認・否認が終わるまで下書きやキューへ追加しない。
  - 「生成開始」で生成APIを開始した直後、モーダルを閉じてメニューの「生成フロー」画面へ自動移動。
- `src/app/api/generation-flow/approve/route.ts`
  - `autoSave` に対応し、STEP13.5/14へ下書き自動保存と確認待ちの状態を記録。
  - 同じ生成セッションを再送しても既存下書きを再利用し、重複作成しない。
- `src/components/GenerationFlowPage.tsx`
  - `approved` 状態を「下書き保存済み」と表示。
  - STEP13.5「ユーザー最終確認」を選択しても右側の最終稿を消さず、読みやすい文字サイズで表示。
  - 承認待ちセッションでは、最終稿の直上に「この最終稿を承認して下書きに保存」を表示し、承認後は保存済み状態を明示。
  - 最終稿を下書きカード風に投稿単位で表示し、ツリーは各コマを別々の入力欄で修正可能にする。
  - 各投稿カードに指摘・修正メモ欄、個別承認ボタン、個別否認ボタンを表示。
  - 全投稿の判定後、承認された投稿だけをまとめて下書きへ保存。
  - 否認ボタンを押した投稿カードはその場で一覧から削除し、指摘内容だけを否認履歴へ残す。
  - 全投稿を否認した場合も「すべて否認して終了」で下書きを作らず完了可能。
- `src/lib/post-parser.ts`
  - 投稿アイテム先頭の `[L1]` / `[L2]` / `[L3]` を除去し、Layer情報を投稿本文へ保存しない。
- `src/app/api/generation-flow/approve/route.ts`
  - 自動保存済み下書きへの本文再反映に対応。
  - 投稿数・ツリーのコマ数が変わる更新、キュー済み・投稿済み本文の更新は拒否。
- `src/app/api/posts/ai-edit/route.ts`
  - DB保存前の編集中本文を `currentBody` として受け取り、複数回のAI修正を順番に重ねられるようにする。
- `src/components/PostCard.tsx`
  - 下書きの操作名を「編集・AI修正」に変更。
  - AI修正案を採用した時だけ、指示・修正前本文・修正後本文を保存時まで保持。
- `src/app/api/posts/group-action/route.ts`
  - 本文保存と採用済みAI修正指示の候補作成を同じトランザクションで実行。
  - 同じ投稿・指示・修正後本文の未確認候補は重複作成しない。
- `prisma/schema.prisma`
  - `KnowledgeRevisionSuggestion` を追加し、候補の `pending / applied / skipped` 状態を保持。
- `prisma/migrations/20260628152000_add_knowledge_revision_suggestions/migration.sql`
  - 既存DBを初期化せず候補テーブルと索引だけを追加。
- `src/components/KnowledgeSuggestionReview.tsx`
  - 下書き画面に未確認候補を古い順で一件ずつ表示。
  - ルール文を編集し、新規ナレッジ作成または既存の追加ナレッジ更新を選択可能。
  - 「この内容を反映」または「今回は見送る」を明示的に押すまで次へ進めない。
- `src/app/api/knowledge/suggestions/route.ts`
  - 未確認候補の取得、見送り、明示的なナレッジ反映に対応。
  - 製品デフォルトおよび別アカウントのナレッジは更新しない。
- `src/components/SettingsPage.tsx`
  - 通常ナレッジの初期表示を「投稿生成フロー順」にし、最初に参照されるSTEPごとに整理。
  - 同じSTEP内では優先度順、生成フローに紐づかない追加ナレッジは末尾に表示。
  - フラット表示でも最初に参照されるSTEP順を維持し、各カードに全参照STEPを表示。
- `src/components/Sidebar.tsx` / `src/components/Dashboard.tsx`
  - 「システム設定」を「アカウント設定」へ改名し、アカウント関連項目だけを表示。
  - 新しいメニュー「ナレッジ」を追加し、その中に「ナレッジ」「専門ナレッジ」の2タブを配置。

### 再適用時に確認すること

- [ ] 生成完了後はSTEP13.5へ移動でき、承認前の投稿は下書きへ保存されない。
- [ ] 保存後に下書きを開き、「編集・AI修正」から自然文で本文を修正できる。
- [ ] 2回目以降のAI修正が直前の修正済み本文を対象にする。
- [ ] AI案を採用せず閉じた場合や手修正だけの場合は、ナレッジ反映候補が作られない。
- [ ] AI案を採用して本文を保存した場合だけ、下書き画面に候補が表示される。
- [ ] 候補が複数ある場合も一件ずつ確認され、反映または見送り後に次の候補が表示される。
- [ ] ユーザー操作なしでナレッジ本文が変更されない。
- [ ] 新規ナレッジと既存の追加ナレッジの両方へ反映でき、デフォルトや別アカウントには反映できない。
- [ ] 承認した投稿だけが下書きへ保存され、その後は従来どおり日時指定してキューへ追加できる。
- [ ] STEP13.5「ユーザー最終確認」を選択したあとも、右側で最終稿を確認できる。
- [ ] 承認待ちでは最終稿の直上に承認ボタンがあり、押すと一度だけ下書きへ保存される。
- [ ] 最終確認で投稿ごと、ツリーの各コマごとに本文を修正できる。
- [ ] 各投稿に指摘欄・承認ボタン・否認ボタンが表示される。
- [ ] 全投稿を判定するまで保存できず、否認した投稿は下書きへ保存されない。
- [ ] 否認ボタンを押すと対象の投稿カードが一覧から消える。
- [ ] 承認0件・全件否認でもエラーにならず、下書きを作らずに生成を終了できる。
- [ ] 最終確認画面と保存後の投稿本文に `[L1]` / `[L2]` / `[L3]` が含まれない。
- [ ] ナレッジタブの初期表示がSTEP0から投稿生成フロー順に並ぶ。
- [ ] 複数STEPで使うナレッジは最初の参照STEPに配置され、カード上で全参照STEPを確認できる。
- [ ] 「生成開始」を押すと生成フロー画面へ移り、進行中のSTEPと参照ナレッジが表示される。
- [ ] メニューに「アカウント設定」と「ナレッジ」が別々に表示される。
- [ ] 「ナレッジ」の中に通常ナレッジと専門ナレッジのタブがある。
