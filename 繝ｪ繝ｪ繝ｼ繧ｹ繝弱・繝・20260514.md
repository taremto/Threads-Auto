# 2026/05/14 最新版アップデート

## 対象

- これから初めてセットアップする人
- すでに前のバージョンを使っている人
- クラウドオフロードを使っている人

配布zipは全員同じです。初めて使う人は「初回セットアップ」、すでに使っている人は「アップデート」を実行してください。

## 主な改善

- Google Apps Script のタイムゾーンを Asia/Tokyo で確実に確認
- GASトリガー、トークン自動更新、同期状態をWebUIで診断
- Threadsアクセストークンの自動更新を強化
- 予約投稿の暴発防止を強化
  - 同じアカウントの投稿は必ず1時間以上空ける
  - 一括予約でも既存予約/投稿から前後1時間以内を避ける
  - GAS側でも1回の実行で1予約だけ処理
- Claude Code CLI のWindows/Mac対応を改善
- API課金につながる環境変数がある場合はAI生成を停止
- Claude利用上限時の初心者向けメッセージを改善
- 自動投稿チェックとサポート用レポートを追加
- 30アカウント規模でも使いやすいようにサイドバーと予約処理を改善
- 小さいブラウザ幅で画面が崩れにくいように調整
- zip解凍時に日本語ファイル名が文字化けしても使えるよう、英数字名の起動ファイルも同梱

## 既存ユーザー向け注意

前のフォルダを消さずに、新しいzipを展開してください。

新しいフォルダの中にある `アップデート.command` または `アップデート.bat` を実行すると、前のフォルダの `prisma/dev.db` を引き継ぎます。

クラウドオフロードを使っている人は、アップデート後に `クラウドオフロード設定.command` または `クラウドオフロード設定.bat` も実行してください。GAS側のコード更新とトリガー確認を反映するためです。

ファイル名が文字化けして見える場合は、以下の英数字名を使ってください。

- 初回セットアップ: `setup.command` / `setup.bat`
- アプリ起動: `start.command` / `start.bat`
- アップデート: `update.command` / `update.bat`
- クラウドオフロード設定: `cloud-setup.command` / `cloud-setup.bat`
