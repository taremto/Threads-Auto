# アップデート手順

この手順は、すでに前のバージョンを使っている人向けです。

## 重要

前のフォルダは消さないでください。

アカウント、投稿、下書き、予約、ナレッジ、クラウドオフロード設定は、前のフォルダの中にある `prisma/dev.db` に入っています。

## 1. 最新zipをダウンロードする

Google Driveから最新版zipをダウンロードしてください。

ファイル名の例:

`nuko-threads-webapp-v1.1.0_20260514.zip`

## 2. 新しいフォルダとして展開する

古いフォルダに上書きしないでください。

新しいzipは、新しいフォルダとして展開します。

## 3. アップデートを実行する

新しいフォルダの中にある以下を開きます。

Mac:

`アップデート.command`

もしファイル名が文字化けして見える場合は、代わりに `update.command` を開いてください。

Windows:

`アップデート.bat`

もしファイル名が文字化けして見える場合は、代わりに `update.bat` を開いてください。

途中で「前のフォルダのパス」を聞かれたら、前のバージョンのフォルダを指定してください。

## 4. 起動する

アップデート完了後、新しいフォルダの `アプリ起動.command` または `アプリ起動.bat` を使って起動してください。

## 5. クラウドオフロード利用者は追加で実行

PCを閉じても投稿されるクラウドオフロードを使っている人は、アップデート後に以下も実行してください。

Mac:

`クラウドオフロード設定.command`

文字化けしている場合は `cloud-setup.command`

Windows:

`クラウドオフロード設定.bat`

文字化けしている場合は `cloud-setup.bat`

これにより、Google Apps Script側の最新修正、タイムゾーン確認、トリガー確認、トークン自動更新が反映されます。

## 6. 動作確認

アプリを開いたら、システム設定 → アカウント → 自動投稿チェックを見てください。

赤い表示が出ていなければ、基本的にはそのまま使えます。

問題がある場合は「サポート用レポートをコピー」を押して、Discordサポートに貼ってください。
