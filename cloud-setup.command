#!/bin/bash
cd "$(dirname "$0")" || { echo "フォルダの場所が分かりませんでした。"; read -r -n 1 -s; exit 1; }
bash setup-cloud.sh
echo
echo "===================================================="
echo " 終わりました。このウィンドウは閉じて大丈夫です。"
echo " （何かキーを押すと閉じます）"
echo "===================================================="
read -r -n 1 -s

