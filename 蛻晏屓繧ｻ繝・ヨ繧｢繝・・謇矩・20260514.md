# 初回セットアップ手順

この手順は、これから初めて使う人向けです。

## 1. zipをダウンロードする

Google Driveから最新版zipをダウンロードしてください。

ファイル名の例:

`nuko-threads-webapp-v1.1.0_20260514.zip`

## 2. zipを展開する

ダウンロードしたzipを展開します。

展開したフォルダは、デスクトップなど分かりやすい場所に置いてください。

## 3. 初回セットアップを実行する

Macの人:

`初回セットアップ.command` を開きます。

もしファイル名が文字化けして見える場合は、代わりに `setup.command` を開いてください。

Windowsの人:

`初回セットアップ.bat` を開きます。

もしファイル名が文字化けして見える場合は、代わりに `setup.bat` を開いてください。

途中で確認が出たら、画面の案内に従って進めてください。

## 4. アプリを起動する

セットアップ完了後、そのまま起動するか聞かれます。

あとで起動する場合は、以下を開いてください。

Mac:

`アプリ起動.command`

文字化けしている場合は `start.command`

Windows:

`アプリ起動.bat`

文字化けしている場合は `start.bat`

## 5. ブラウザで開く

ブラウザで以下を開きます。

`http://localhost:3000`

## 6. アカウントを追加する

システム設定からThreadsアカウントを追加します。

アクセストークン、投稿時間帯、1日の投稿数、コンセプトを設定してください。

## 7. AI生成を使う

AI生成はClaudeの月額プラン内で使う想定です。

APIキーによる従量課金設定が見つかった場合、このツールは安全のため生成を止めます。

## 8. PCを閉じても投稿したい場合

クラウドオフロードを設定してください。

Mac:

`クラウドオフロード設定.command`

文字化けしている場合は `cloud-setup.command`

Windows:

`クラウドオフロード設定.bat`

文字化けしている場合は `cloud-setup.bat`
